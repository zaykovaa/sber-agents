# Идея: RAG-ассистент на базе LangChain

## Описание проекта

RAG-ассистент (Retrieval-Augmented Generation) реализован в виде Telegram-бота, который использует базу знаний из документов для предоставления точных и релевантных ответов.

## Основная задача

Бот должен вести диалог с пользователем и отвечать на его вопросы, используя RAG-подход:
- Поиск релевантной информации в базе знаний (PDF-документы)
- Генерация ответа на основе найденного контекста и истории диалога
- Понимание уточняющих вопросов с учетом контекста беседы

## Ключевые особенности

- **Платформа**: Telegram
- **Технология**: RAG (Retrieval-Augmented Generation) на базе LangChain
- **База знаний**: PDF-документы из директории data/
- **Функционал**: 
  - Интеллектуальный поиск по документам
  - Контекстные ответы с учетом истории диалога
  - Трансформация уточняющих вопросов
  - Управление индексацией документов
  - Отображение источников информации (опционально)
  - Интеграция с LangSmith для трейсинга и мониторинга
  - Автоматическая оценка качества через RAGAS метрики
  - Синтез тестовых датасетов на основе документов
- **Поведение**: Ответы формируются на основе документов и контекста беседы

## Новые возможности (Спринт 3)

### Отображение источников
Бот может показывать пользователю список документов и страниц, которые были использованы для формирования ответа. Режим включается через конфигурацию `SHOW_SOURCES=true`.

**Пример:**
```
Ответ бота на вопрос...

📚 Источники: 
• ouk_potrebitelskiy_kredit_lph.pdf (стр. 3, 5)
• usl_r_vkladov.pdf (стр. 12)
```

### Мониторинг через LangSmith
Все запросы к RAG системе автоматически логируются в LangSmith при установке соответствующих переменных окружения. Это позволяет:
- Просматривать детальные traces каждого запроса
- Анализировать производительность и стоимость
- Отлаживать проблемы в RAG пайплайне
- Мониторить качество ответов в реальном времени

### Оценка качества
Команда `/evaluate-dataset [название]` запускает автоматическую оценку качества RAG системы на тестовом датасете из LangSmith.

**Метрики RAGAS:**
- `faithfulness` - обоснованность ответа (нет галлюцинаций)
- `answer_relevancy` - релевантность ответа вопросу
- `answer_correctness` - правильность относительно эталона
- `answer_similarity` - семантическая похожесть на эталон
- `context_recall` - полнота извлеченного контекста (все ли нужные факты найдены)
- `context_precision` - точность извлеченного контекста (только релевантные документы)


Результаты оценки сохраняются как feedback в LangSmith для дальнейшего анализа.

### Синтез датасетов
Автоматическое создание тестовых датасетов на основе имеющихся документов:
- Выбор репрезентативных фрагментов (по 2 на файл)
- Генерация вопросов и эталонных ответов через LLM
- Сохранение в формате JSON
- Загрузка в LangSmith для evaluation

**Команды:**
- `make dataset` - создать датасет локально
- `make dataset-upload` - загрузить в LangSmith

