# Отчет: ReAct Agent с инструментами для банковского ассистента

## 1. Реализованные инструменты

### 1.1 Основной инструмент поиска (был изначально)
- **`rag_search(query: str)`** - Поиск информации в документах Сбербанка
  - Использует гибридный RAG (semantic/hybrid/hybrid_reranker)
  - Возвращает JSON с релевантными документами и их источниками
  - Поддерживает PDF и JSON документы

### 1.2 Новые инструменты для расчетов (реализованы дополнительно)

#### `calculate_loan_payment(principal, annual_rate, months)`
- **Назначение**: Расчет ежемесячного платежа по аннуитетному кредиту
- **Параметры**:
  - `principal`: Сумма кредита (руб.)
  - `annual_rate`: Годовая процентная ставка (%)
  - `months`: Срок кредита (месяцы)
- **Возвращает**: JSON с ежемесячным платежом, общей суммой выплат и переплатой
- **Формула**: Использует стандартную формулу аннуитетного платежа
  ```
  A = P * (r * (1 + r)^n) / ((1 + r)^n - 1)
  ```

#### `calculate_deposit_interest(principal, annual_rate, days, capitalization)`
- **Назначение**: Расчет дохода по вкладу с учетом процентов
- **Параметры**:
  - `principal`: Сумма вклада (руб.)
  - `annual_rate`: Годовая процентная ставка (%)
  - `days`: Срок вклада (дни)
  - `capitalization`: Флаг капитализации процентов (bool)
- **Возвращает**: JSON с доходом и итоговой суммой
- **Формулы**: 
  - С капитализацией: `A = P * (1 + r)^n` (сложные проценты)
  - Без капитализации: `A = P * (1 + r * n)` (простые проценты)

#### `calculate_percentage(amount, percentage)`
- **Назначение**: Универсальный калькулятор процентов
- **Параметры**:
  - `amount`: Исходная сумма (руб.)
  - `percentage`: Процент (%)
- **Возвращает**: JSON с результатом расчета процента от суммы

**Всего инструментов**: 4 (1 поисковый + 3 расчетных)

## 2. Как агент принимает решения о вызове инструментов

### 2.1 Архитектура принятия решений

Агент использует **ReAct паттерн** (Reasoning + Acting) через `create_agent()` из LangChain 1.0:

```
1. Reasoning (Рассуждение)
   ↓
2. Acting (Действие - вызов инструмента)
   ↓
3. Observation (Наблюдение результата)
   ↓
4. Repeat до получения финального ответа
```

### 2.2 Механизм принятия решений

#### Роль LLM в принятии решений
- LLM (например, GPT-4o) анализирует запрос пользователя
- Оценивает доступные инструменты (их описания передаются через декоратор `@tool`)
- Принимает решение: нужно ли вызывать инструмент, какой именно, и с какими параметрами

#### Описание инструментов для LLM
```python
@tool
def calculate_loan_payment(...) -> str:
    """
    Рассчитывает ежемесячный платеж по аннуитетному кредиту.
    ...
    """
```

LangChain автоматически извлекает docstring и сигнатуру функции, формируя описание для LLM.

#### Процесс принятия решения

1. **Получение запроса**: Агент получает `HumanMessage` с вопросом пользователя
2. **Анализ контекста**: 
   - Агент анализирует вопрос
   - Проверяет историю диалога (из MemorySaver)
   - Оценивает, нужна ли дополнительная информация
3. **Выбор действия**:
   - **Вариант A**: Если достаточно информации в контексте → формирует ответ напрямую
   - **Вариант B**: Если нужна информация → вызывает `rag_search`
   - **Вариант C**: Если нужен расчет → вызывает соответствующий калькулятор
   - **Вариант D**: Комбинация инструментов (например, сначала поиск, потом расчет)
4. **Обработка результата**: 
   - Агент получает результат инструмента (ToolMessage)
   - Анализирует его
   - Решает: достаточно ли информации или нужно вызвать еще инструменты
5. **Формирование ответа**: Когда информации достаточно, агент формирует финальный ответ пользователю

### 2.3 Примеры решений агента

#### Пример 1: Простой вопрос без инструментов
```
Пользователь: "Привет!"
Агент: Анализирует → не требует дополнительной информации → отвечает напрямую
```

#### Пример 2: Вопрос требует поиска
```
Пользователь: "Какие условия потребительского кредита?"
Агент: 
  1. Анализирует → нужна информация из документов
  2. Вызывает rag_search("условия потребительского кредита")
  3. Получает документы
  4. Формирует ответ на основе найденных данных
```

#### Пример 3: Вопрос требует расчета
```
Пользователь: "Рассчитай платеж по кредиту 1 млн на 5 лет под 15%"
Агент:
  1. Анализирует → нужен расчет
  2. Извлекает параметры: principal=1000000, rate=15, months=60
  3. Вызывает calculate_loan_payment(1000000, 15, 60)
  4. Получает результат расчета
  5. Формирует понятный ответ с объяснением
```

#### Пример 4: Комбинация инструментов
```
Пользователь: "Сколько будет платеж по потребительскому кредиту 500 тысяч на 3 года?"
Агент:
  1. Анализирует → нужна ставка по кредиту
  2. Вызывает rag_search("ставка потребительский кредит")
  3. Получает ставку (например, 18%)
  4. Вызывает calculate_loan_payment(500000, 18, 36)
  5. Формирует ответ с расчетом
```

### 2.4 Системный промпт и правила

Системный промпт (`prompts/agent_system.txt`) содержит правила:
- Когда использовать `rag_search` (вопросы о продуктах, условиях)
- Когда НЕ использовать (приветствия, благодарности)
- Как формировать ответы (вежливо, конкретно, на русском)

Эти правила направляют агента, но финальное решение принимает LLM на основе контекста.

## 3. Отличия агента от простой RAG-цепочки

### 3.1 Простая RAG-цепочка

**Архитектура:**
```
Запрос пользователя
    ↓
Векторный поиск (retrieval)
    ↓
Контекст + Запрос → LLM
    ↓
Ответ
```

**Характеристики:**
- Всегда выполняет поиск в документах
- Линейный процесс: запрос → поиск → генерация
- Нет принятия решений
- Нет возможности делать расчеты
- Ограничена только поиском + генерацией

### 3.2 ReAct Agent

**Архитектура:**
```
Запрос пользователя
    ↓
LLM анализирует и принимает решение
    ↓
[Выбор действия]
    ├─ Отвечать напрямую (если достаточно контекста)
    ├─ Вызвать rag_search (если нужны документы)
    ├─ Вызвать calculate_* (если нужны расчеты)
    └─ Комбинация инструментов
    ↓
Обработка результатов
    ↓
[Достаточно информации?]
    ├─ Да → Формирование финального ответа
    └─ Нет → Повтор цикла с дополнительными инструментами
    ↓
Ответ пользователю
```

**Ключевые отличия:**

| Аспект | Простая RAG | ReAct Agent |
|--------|------------|-------------|
| **Принятие решений** | Нет, всегда поиск | Да, LLM решает что делать |
| **Гибкость** | Только поиск | Множество инструментов |
| **Контекст** | Только из документов | История диалога + документы + результаты расчетов |
| **Многошаговость** | Один шаг | Может выполнять несколько шагов |
| **Вычисления** | Не поддерживает | Поддерживает через инструменты |
| **Умные ответы** | Только на основе документов | Может отвечать без поиска (приветствия, общие вопросы) |
| **Эффективность** | Всегда делает поиск (даже когда не нужен) | Выполняет поиск только когда нужно |

### 3.3 Конкретные примеры отличий

#### Пример 1: Приветствие
- **RAG-цепочка**: Выполнит поиск по "привет", не найдет релевантного, попытается сгенерировать ответ
- **Agent**: Поймет, что это приветствие, ответит напрямую без поиска

#### Пример 2: Расчет без документов
```
Пользователь: "Посчитай 20% от 100 тысяч"
```
- **RAG-цепочка**: Выполнит поиск, не найдет полезного контекста, не сможет сделать расчет
- **Agent**: Вызовет `calculate_percentage(100000, 20)` и вернет точный результат

#### Пример 3: Сложный запрос с несколькими шагами
```
Пользователь: "Сколько будет платеж по кредиту 500 тысяч на 3 года?"
```
- **RAG-цепочка**: 
  1. Поиск в документах
  2. Попытка найти готовую информацию о платеже
  3. Может не найти точный ответ для этой суммы
- **Agent**:
  1. Понимает, что нужен расчет
  2. Вызывает `rag_search` для получения процентной ставки
  3. Вызывает `calculate_loan_payment` с найденной ставкой
  4. Формирует точный ответ с расчетом

## 4. Преимущества и ограничения

### 4.1 Преимущества ReAct Agent

#### 🧠 Интеллектуальное принятие решений
- Агент сам решает, когда использовать инструменты
- Не тратит ресурсы на ненужные вызовы (например, поиск для приветствия)
- Более естественное поведение

#### 🔄 Многошаговое рассуждение
- Может выполнять несколько шагов для решения задачи
- Комбинирует разные инструменты (поиск + расчет)
- Может уточнять запросы и переформулировать вопросы

#### 🛠️ Расширяемость
- Легко добавлять новые инструменты (просто создать функцию с `@tool`)
- Агент автоматически получает описание нового инструмента
- Не требует изменения основной логики

#### 💬 Естественные диалоги
- Поддерживает контекст диалога (MemorySaver)
- Может отвечать на общие вопросы без поиска
- Понимает контекст предыдущих сообщений

#### 🎯 Точность расчетов
- Математические вычисления выполняются в коде, а не LLM
- Гарантированная точность формул
- Нет галлюцинаций в расчетах

#### 📊 Комбинированное использование
- Может искать информацию и делать расчеты
- Пример: найти ставку в документах, затем рассчитать платеж

### 4.2 Ограничения и проблемы

#### ⏱️ Задержка выполнения
- Многошаговый процесс занимает больше времени
- Каждый вызов инструмента добавляет задержку
- Несколько LLM вызовов вместо одного

**Пример:**
- Простая RAG: 1-2 секунды
- Agent с поиском + расчетом: 3-5 секунд

#### 💰 Стоимость API вызовов
- Каждый шаг рассуждения = вызов LLM
- Несколько инструментов = несколько шагов = больше токенов
- Дороже чем простая RAG-цепочка

**Оценка:**
- Простая RAG: ~2000-3000 токенов на запрос
- Agent (поиск + расчет): ~4000-6000 токенов на запрос

#### 🎲 Непредсказуемость
- LLM может принять неправильное решение
- Может вызвать ненужный инструмент
- Может не вызвать нужный инструмент
- Требует тщательной настройки системного промпта

**Примеры проблем:**
- Агент может вызвать поиск для простого вопроса
- Агент может не понять, что нужен расчет
- Агент может неправильно извлечь параметры для расчета

#### 🔍 Сложность отладки
- Сложнее отследить путь выполнения
- Нужно логировать каждый шаг (что и делается в `_log_agent_step`)
- Больше мест, где может возникнуть ошибка

#### 📝 Зависимость от качества промпта
- Системный промпт критически важен
- Плохой промпт = плохое принятие решений
- Требует итеративной настройки

#### 🧮 Ограничения LLM для извлечения параметров
- LLM может неправильно извлечь числа из запроса
- Например: "500 тысяч" → может интерпретировать как 500 или 500000
- Требует валидации параметров в инструментах

#### 🔄 Риск бесконечных циклов
- Теоретически агент может вызывать инструменты в цикле
- LangChain имеет ограничения на количество шагов, но это нужно настраивать
- Требует мониторинга

### 4.3 Когда использовать Agent vs простую RAG

#### ✅ Используйте Agent когда:
- Нужны сложные многошаговые задачи
- Требуются вычисления и расчеты
- Важна естественность диалога (ответы без поиска на общие вопросы)
- Нужна комбинация поиска и вычислений
- Запросы могут быть неоднозначными

#### ❌ Используйте простую RAG когда:
- Все запросы требуют только поиска в документах
- Нужна максимальная скорость
- Важна минимальная стоимость
- Запросы простые и однозначные
- Нет необходимости в вычислениях

### 4.4 Рекомендации по улучшению

1. **Валидация параметров**: Добавить проверку входных параметров в инструментах
2. **Ограничение шагов**: Установить максимальное количество шагов агента
3. **Кэширование**: Кэшировать результаты частых запросов
4. **Мониторинг**: Отслеживать количество вызовов инструментов и время выполнения
5. **Обработка ошибок**: Улучшить обработку ошибок в инструментах
6. **Тестирование**: Добавить тесты для проверки правильности принятия решений
7. **Улучшение промпта**: Итеративно улучшать системный промпт на основе реальных диалогов

## 5. Выводы

ReAct Agent представляет собой значительное улучшение по сравнению с простой RAG-цепочкой, предоставляя:

- **Интеллектуальное принятие решений** о необходимости использования инструментов
- **Гибкость** в работе с различными типами задач (поиск, расчеты, комбинации)
- **Естественность** в диалогах благодаря пониманию контекста

Однако это достигается за счет:
- **Увеличения сложности** системы
- **Роста стоимости** (больше вызовов LLM)
- **Увеличения времени** выполнения

**Итоговая оценка**: Agent подходит для production-систем, где важна гибкость и естественность взаимодействия, но требует тщательной настройки и мониторинга.

