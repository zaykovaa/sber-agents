# 🎬 Telegram Bot - Эксперт кино

Telegram-бот с интеграцией LLM, который ведет диалог с пользователями в роли **Эксперта по кино и сериалам**.

## 🎭 Описание

Бот помогает пользователям:
- Найти идеальный фильм или сериал для просмотра
- Узнать о последних трендах и релизах
- Получить рекомендации по жанрам
- Обсудить кинематографические шедевры
- Получить краткие описания без спойлеров

## 📋 Требования

- Python 3.10+
- [uv](https://github.com/astral-sh/uv) (менеджер пакетов)
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- OpenRouter API Key

## 🚀 Установка и запуск

### 1. Перейдите в папку проекта

```bash
cd 04-multimodal
```

### 2. Установите зависимости

```bash
make setup
```

или вручную:

```bash
uv sync
```

### 3. Создайте файл `.env`

Создайте файл `.env` в корне проекта и добавьте необходимые ключи:

```bash
# OpenRouter API Configuration
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
MODEL_NAME=openai/gpt-3.5-turbo

# Vision model for image processing (optional, defaults to MODEL_NAME)
# Используйте только модели, поддерживающие vision:
# • openai/gpt-4o-mini (рекомендуется)
# • google/gemini-pro-vision
# • anthropic/claude-3-haiku
# • anthropic/claude-3-5-sonnet
MODEL_IMAGE=openai/gpt-4o-mini

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Bot Settings
MAX_HISTORY_MESSAGES=10

# System Prompts (optional, defaults to prompts/ folder)
# Пути к файлам с промптами или напрямую через переменные:
# SYSTEM_PROMPT_TEXT_PATH=prompts/system_prompt_text.txt
# SYSTEM_PROMPT_IMAGE_PATH=prompts/system_prompt_image.txt

```

**Где получить ключи:**

- **TELEGRAM_BOT_TOKEN**: Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
- **OPENROUTER_API_KEY**: Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai) и получите API ключ
### 4. Запустите бота

```bash
make run
```

или вручную:

```bash
uv run python src/bot.py
```

## 📱 Использование

1. Найдите вашего бота в Telegram по имени, которое вы указали в BotFather
2. Отправьте команду `/start`
3. Начните диалог с любым вопросом о кино или сериалах!

**Примеры запросов:**
- "Что посмотреть сегодня вечером?"
- "Рекомендуй хорошие триллеры"
- "Какие новинки вышли в этом месяце?"
- "Стоит ли смотреть 'Интерстеллар'?"
- "Расскажи о современных трендах в сериальном производстве"

**Команды бота:**
- `/start` — начать новую сессию
- `/clear` — очистить историю диалога
- `/help` — показать справку

**Обработка изображений:**
- Бот может анализировать изображения (постеры, скриншоты фильмов и т.д.)
- Отправьте фото с подписью или без — бот проанализирует его
- Для работы с изображениями настройте `MODEL_IMAGE` в `.env` (см. ниже)

## 🎯 Особенности

- **Роль Эксперта кино**: Специальный системный промпт определяет поведение бота как профессионального консультанта по фильмам
- **Обработка изображений**: Анализ постеров, скриншотов и других изображений через vision-модели
- **История диалога**: Бот помнит контекст предыдущих сообщений в рамках сессии
- **Управление памятью**: Автоматическое ограничение длины истории для оптимальной работы
- **Многопользовательская поддержка**: Отдельная история для каждого пользователя
- **Метрики и логирование**: Детальное логирование для отладки и мониторинга

## 🔧 Структура проекта

```
04-multimodal/
├── src/
│   ├── __init__.py
│   ├── bot.py              # Главный файл запуска бота
│   ├── config.py           # Конфигурация и загрузка промптов
│   ├── storage.py          # Хранение истории диалогов
│   ├── handlers/           # Обработчики сообщений
│   │   ├── __init__.py
│   │   ├── commands.py     # Обработчики команд (/start, /help, /clear)
│   │   ├── text.py         # Обработчик текстовых сообщений
│   │   ├── image.py        # Обработчик изображений
│   └── services/           # Сервисы для работы с внешними API
│       ├── __init__.py
│       ├── llm.py          # Сервис для работы с LLM
│       └── image.py        # Сервис для обработки изображений
├── prompts/                # Системные промпты
│   ├── system_prompt_text.txt   # Промпт для текстовых сообщений
│   └── system_prompt_image.txt  # Промпт для обработки изображений
├── docs/                   # Документация проекта
├── .env                    # Конфигурация (создайте самостоятельно)
├── .env.example            # Пример конфигурации
├── .gitignore              # Git ignore правила
├── Makefile                # Команды для удобной работы
├── pyproject.toml          # Конфигурация проекта
└── README.md               # Этот файл
```

## 🛠️ Доступные команды

```bash
make setup  # Установить зависимости
make run    # Запустить бота
make clean  # Очистить временные файлы
make help   # Показать справку
```

## 📝 Системные промпты

Бот использует системные промпты, которые определяют его роль как эксперта по кино. Промпты хранятся в папке `prompts/`:

- **system_prompt_text.txt** — промпт для текстовых сообщений
- **system_prompt_image.txt** — промпт для обработки изображений

### Редактирование промптов

1. **Через файлы** (рекомендуется):
   - Отредактируйте файлы в папке `prompts/`
   - Перезапустите бота

2. **Через переменные окружения**:
   ```bash
   SYSTEM_PROMPT_TEXT=твой текстовый промпт
   SYSTEM_PROMPT_IMAGE=твой промпт для изображений
   ```

3. **Через пути к файлам**:
   ```bash
   SYSTEM_PROMPT_TEXT_PATH=путь/к/файлу.txt
   SYSTEM_PROMPT_IMAGE_PATH=путь/к/файлу.txt
   ```

Промпты загружаются автоматически при старте бота через `config.py`.

## 🔒 Безопасность

- Не коммитьте файл `.env` в репозиторий!
- Храните API ключи в безопасности
- Используйте переменные окружения для конфигурации

## 🐛 Отладка

Логи выводятся в консоль с уровнем INFO. Для более детальной отладки измените уровень логирования в `src/bot.py`:

```python
logging.basicConfig(level=logging.DEBUG)
```

## 📊 Метаданные и статистика

Бот отслеживает:
- Общее количество пользователей
- Общее количество сообщений
- Время запуска
- Использование токенов (в логах)

## 🎓 Образовательная цель

Этот проект создан в рамках курса "AI-driven разработка ИИ-агентов" и демонстрирует:
- Интеграцию Telegram Bot API с LLM
- Работу с системными промптами для определения роли
- Управление историей диалога и контекстом
- Многопользовательскую архитектуру

## 📚 Зависимости

- `openai` — клиент для работы с OpenRouter API
- `python-dotenv` — загрузка переменных окружения
- `aiogram` — асинхронный фреймворк для создания Telegram ботов

## 📄 Лицензия

Образовательный проект для курса Sber Agents.

---

**Автор**: Зайкова Анастасия Тимуровна  
**Проект**: 04-multimodal - Мультимодальный Telegram бот с обработкой изображений

