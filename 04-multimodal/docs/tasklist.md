# План разработки Telegram Bot - Эксперт кино

## 📊 Отчет по прогрессу

| Этап | Статус | Прогресс |
|------|--------|----------|
| 🎯 Подготовка окружения | ✅ | 100% |
| 🤖 Базовый бот | ✅ | 100% |
| 💬 Интеграция с LLM | ✅ | 100% |
| 🗣️ История диалога | ✅ | 100% |
| 📝 Системный промпт | ✅ | 100% |
| 🎬 Финальная отладка | ✅ | 100% |
| 🏗️ Модульная архитектура | ✅ | 100% |
| 🖼️ Обработка изображений | ✅ | 100% |

**Легенда:**
- ⏳ Не начато
- 🔄 В процессе
- ✅ Завершено
- ❌ Пропущено

---

## Итерация 1: Подготовка окружения

**Цель:** Настроить проект, установить зависимости, создать конфигурацию

- [x] Обновить `pyproject.toml` — заменить `python-telegram-bot` на `aiogram`
- [x] Создать `src/bot.py` с базовой структурой класса
- [x] Создать `.env.example` с шаблоном конфигурации
- [x] Настроить логирование
- [x] Проверить установку зависимостей: `make setup`
- [x] **Проверка:** код запускается без ошибок

---

## Итерация 2: Базовый бот

**Цель:** Запустить Telegram бота с простыми командами

- [x] Инициализировать aiogram Bot и Dispatcher
- [x] Реализовать метод `run()` для запуска polling
- [x] Добавить обработчик `/start` с приветствием
- [x] Добавить обработчик `/help` со справкой
- [x] Настроить базовое логирование запуска
- [x] **Проверка:** бот отвечает на команды /start и /help в Telegram

---

## Итерация 3: Интеграция с LLM

**Цель:** Подключить LLM через OpenRouter API

- [x] Инициализировать AsyncOpenAI клиент с OpenRouter
- [x] Реализовать метод `generate_response(user_id, text)`
- [x] Добавить обработку ошибок LLM
- [x] Логировать использование токенов
- [x] **Проверка:** бот может вызвать LLM и получить ответ

---

## Итерация 4: История диалога

**Цель:** Сохранять контекст разговора для каждого пользователя

- [x] Создать `conversations: Dict[int, List[Dict]]` в памяти
- [x] Реализовать `get_conversation_history(user_id)`
- [x] Реализовать `add_message(user_id, role, content)`
- [x] Добавить ограничение длины истории (MAX_HISTORY_MESSAGES)
- [x] Добавить метод `clear_conversation(user_id)`
- [x] Добавить обработчик `/clear` для очистки истории
- [x] **Проверка:** бот помнит контекст в диалоге

---

## Итерация 5: Системный промпт

**Цель:** Настроить роль бота как Эксперта кино

- [x] Добавить системный промпт в константу `SYSTEM_PROMPT`
- [x] Модифицировать `get_conversation_history` для добавления системного промпта
- [x] Убедиться, что системный промпт не теряется при очистке истории
- [x] Обновить `/start` для инициализации истории с системным промптом
- [x] Протестировать диалог на примере вопросов о кино
- [x] **Проверка:** бот отвечает как Эксперт кино

---

## Итерация 6: Финальная отладка

**Цель:** Проверить все сценарии, исправить проблемы

- [x] Протестировать сценарии: /start, /help, /clear, обычные сообщения
- [x] Проверить обработку ошибок при недоступности LLM
- [x] Проверить обработку ошибок при неверной конфигурации
- [x] Обновить статистику (total_users, total_messages)
- [x] Проверить, что история ограничивается корректно
- [x] Убрать отладочные логи, оставить только нужные
- [x] Обновить README.md с финальными инструкциями
- [x] **Проверка:** полный тест всех функций бота

---

## Итерация 7: Модульная архитектура и обработка изображений

**Цель:** Реорганизовать проект в модульную структуру и добавить поддержку обработки изображений

### 7.1 Реорганизация структуры проекта

- [x] Создать модульную структуру проекта:
  - [x] `src/config.py` — конфигурация и загрузка промптов
  - [x] `src/storage.py` — хранение истории диалогов
  - [x] `src/handlers/` — обработчики сообщений
  - [x] `src/services/` — сервисы для работы с внешними API
- [x] Разделить обработчики:
  - [x] `handlers/commands.py` — обработчики команд (/start, /help, /clear)
  - [x] `handlers/text.py` — обработчик текстовых сообщений
  - [x] `handlers/image.py` — обработчик изображений
- [x] Создать сервисы:
  - [x] `services/llm.py` — сервис для работы с LLM
  - [x] `services/image.py` — сервис для обработки изображений
- [x] Обновить `bot.py` для использования новой структуры
- [x] **Проверка:** проект работает с новой модульной структурой

### 7.2 Системные промпты в файлах

- [x] Создать папку `prompts/`
- [x] Создать `prompts/system_prompt_text.txt` — промпт для текстовых сообщений
- [x] Создать `prompts/system_prompt_image.txt` — промпт для обработки изображений
- [x] Обновить `config.py` для загрузки промптов из файлов
- [x] Добавить поддержку переменных окружения для переопределения промптов
- [x] Обновить `storage.py` для использования разных промптов для текста и изображений
- [x] **Проверка:** промпты загружаются из файлов и используются правильно

### 7.3 Обработка изображений

- [x] Реализовать `ImageService`:
  - [x] Метод `download_image()` — загрузка изображения из Telegram
  - [x] Конвертация изображения в base64 формат
  - [x] Определение MIME типа изображения
  - [x] Метод `create_multimodal_content()` — формирование мультимодального сообщения
- [x] Реализовать `ImageHandler`:
  - [x] Обработка фотографий из Telegram
  - [x] Поддержка подписей к изображениям (caption)
  - [x] Интеграция с `ImageService` и `LLMService`
  - [x] Использование vision промпта для изображений
- [x] Обновить `LLMService`:
  - [x] Поддержка параметра `use_vision` для выбора модели
  - [x] Автоматический выбор модели (текстовая или vision)
  - [x] Обработка ошибок для vision моделей
- [x] Зарегистрировать обработчик изображений в `bot.py`
- [x] Обновить конфигурацию для поддержки `MODEL_IMAGE`
- [x] **Проверка:** бот может обрабатывать изображения и анализировать их

### 7.4 Обновление документации

- [x] Обновить `README.md`:
  - [x] Новая структура проекта
  - [x] Информация о папке `prompts/`
  - [x] Инструкции по настройке обработки изображений
  - [x] Обновленная конфигурация `.env`
- [x] Обновить `docs/vision.md`:
  - [x] Модульная архитектура проекта
  - [x] Описание компонентов (handlers, services, config, storage)
  - [x] Потоки данных для текста и изображений
  - [x] Системные промпты из файлов
  - [x] Сценарии обработки изображений
- [x] **Проверка:** документация актуальна и полна

---

## Критерии готовности MVP

### Базовый функционал
- ✅ Бот запускается через `make run`
- ✅ Бот отвечает на команды /start, /help, /clear
- ✅ Бот ведет диалог на тему кино и фильмов
- ✅ История диалога сохраняется во время сессии
- ✅ История не повреждается при ошибках LLM
- ✅ Бот логирует основные события
- ✅ Конфигурация загружается из .env

### Модульная архитектура
- ✅ Проект организован в модульную структуру (handlers, services, config, storage)
- ✅ Разделение ответственности между модулями
- ✅ Код легко читается и поддерживается

### Обработка изображений
- ✅ Бот может получать и обрабатывать изображения из Telegram
- ✅ Изображения конвертируются в формат, подходящий для vision-моделей
- ✅ Используется специальный промпт для анализа изображений
- ✅ Поддерживаются подписи к изображениям (caption)
- ✅ Настроена отдельная модель для обработки изображений (MODEL_IMAGE)

### Системные промпты
- ✅ Промпты хранятся в папке `prompts/` в виде файлов
- ✅ Раздельные промпты для текстовых сообщений и изображений
- ✅ Возможность переопределения промптов через переменные окружения

